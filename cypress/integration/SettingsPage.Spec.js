describe('Settings Page', () => {
    it('Can change root directory', () => {
        throw new Error();
    });

    it('can only access with admin role', () => {
        throw new Error();
    });
});