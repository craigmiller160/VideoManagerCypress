describe('User Management Page', () => {
    it('can only access with admin role', () => {
        throw new Error();
    });

    it('displays all users', () => {
        throw new Error();
    });

    it('can edit existing user', () => {
        throw new Error();
    });

    it('can add existing user', () => {
        throw new Error();
    });
});